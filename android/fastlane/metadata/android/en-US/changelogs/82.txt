• Show the organization's mark in the chrome, not just in one export
• The search button is the tab bar's own, not a second glass
• Lift the floating nav out of Android's navigation bar
• Take the secure-storage fix that stops a use-after-free
• Make the metainfo pass Flathub's validation
• Repair what the security round broke, and finish it
• Answer load() from the bus instead of blocking on it
• Close what the security review found
